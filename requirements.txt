# Cloud Guardian - Python Dependencies
# Install with: pip install -r requirements.txt

# System monitoring
psutil>=5.9.0

# Command line argument parsing (built-in, but explicit for documentation)
argparse

# AWS SDK for Python (optional, for direct CloudWatch integration)
boto3>=1.26.0

# Logging and utilities (built-in, but explicit for documentation)
logging
threading
multiprocessing
datetime
json

# Network testing
requests>=2.28.0

# Development dependencies (optional)
pytest>=7.0.0
black>=22.0.0
flake8>=5.0.0
